hardstatus alwayslastline "%{b kw}%H %{r}%1` %{w}| %{g}%c %{w}| %{y}%Y %M %d %{w}| %{g}%l %{w}| %{-b kw}%u %-Lw%{= rW}%50> %n%f %t %{-}%+Lw%<"

defscrollback 100000


# Hide hardstatus: ctrl-a f
bind f eval "hardstatus ignore"
# # Show hardstatus: ctrl-a F
bind F eval "hardstatus alwayslastline"

# Machine specific tweaks go here after creating a local machien branch
screen bash
title vim
screen bash
screen bash
title morbo
screen psql -d gcis
title db
screen bash
title gcisops
screen tail -f /usr/local/projects/gcis/log/production.log
title drlogs
screen sudo tail -f /var/log/nginx/access.log /var/log/nginx/error.log
title nginx logs
